import { GamesArena } from "@/components/games/games-arena";

// Kivo Games — full-screen arena. Games are played here; chat only ever carries
// a chip that deep-links back to this page.
export default function GamesPage() {
  return <GamesArena />;
}

# Copy to .env (local) — never commit real URLs with secrets.
# Android emulator reaches host backend via 10.0.2.2.
# Physical device: use your LAN IP, e.g. http://192.168.1.5:4000
EXPO_PUBLIC_API_URL=http://10.0.2.2:4000
